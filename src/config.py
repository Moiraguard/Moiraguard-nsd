# src/config.py

# Default network interface to sniff packets
DEFAULT_INTERFACE = "eth0"  # Change to "wlan0" for Wi-Fi interfaces or leave empty for prompt

# Path to the log file
LOG_FILE_PATH = "logs.json"
